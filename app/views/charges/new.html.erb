<div>
  <h3>Sign Up to Be a Premium Blocipedia User</h3>
</div>
<div>
  <h4>Post Unlimited Private Wikis!</h4>    
</div>
<div>
  <%= form_tag charges_path do %>
    <article>
      <% if flash[:error].present? %>
        <div id="error_explanation">
          <p><%= flash[:error] %></p>
        </div>
      <% end %>
      <label class="amount">
        <span>Lifetime Subscription: $15.00</span>
      </label>
    </article>

    <script src="https://checkout.stripe.com/checkout.js" class="stripe-button"
            data-key="<%= Rails.configuration.stripe[:publishable_key] %>"
            data-description="Lifetime Subscription"
            data-amount="1500"
            data-locale="auto"></script>
  <% end %></br></br>
  <button><%= link_to 'No Thanks', root_path, :class => 'button right' %></button>, take me to the Wikis.
</div>