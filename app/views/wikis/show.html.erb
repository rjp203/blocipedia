<h3><%= @wiki.title %></h3>

<% if @wiki.private == true && current_user.role == "standard" %>
  <div>
    <h3>This is a private Wiki.  You must be a Premium member to view.</h3>
    <h4>Click the button below to become a Premium member so you can view and create private Wikis!</h4>
      <button><%= link_to 'Upgrade to Premium', new_charge_path, :class => 'button right' %></button>
        </br></br>
      <button><%= link_to 'No Thanks', root_path, :class => 'button right' %></button>, take me to the public Wikis.
  </div>
<% else %>
  <div>
    <% if @wiki.user %>
      <h6 class="indent"></indent>by <%= @wiki.user.firstname %> <%= @wiki.user.lastname %></h6>
      <% if @wiki.private == true %>
        <h6 class="indent">This Wiki is <strong>Private</strong>.</h6>
      <% end %>
    <% end %>
  </div>

  <div class="row">
    <div class="col-md-8">
      <p><%= @wiki.body %></p>
    </div>
  
    <div class="col-md-4">
      <%= link_to "Edit Wiki", edit_wiki_path(@wiki), class: 'btn btn-success' %>
      <%= link_to "Delete Wiki", @wiki, method: :delete, class: 'btn btn-danger', data: {confirm: 'Are you sure you want to delete this wiki?'} if policy(@wiki).destroy?%>
    </div>
  </div>
<% end %>
